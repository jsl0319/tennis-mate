# Tennis Mate 개발 지침

## 1. 문서 목적

이 파일은 Tennis Mate 저장소에서 작업하는 Codex 및 개발 에이전트가 항상 따라야 할 공통 지침이다.

제품의 상세 요구사항은 `docs/` 아래 문서를 기준으로 하며, 이 파일에는 반복해서 적용할 작업 원칙, 제약조건, 검증 기준만 기록한다.

## 2. 제품 목표

Tennis Mate는 테니스 초보자가 실력 차이에 대한 부담 없이 자신과 비슷한 수준의 사람을 찾고, 함께 테니스 칠 약속을 잡도록 돕는 매칭 웹앱이다.

서비스가 해결해야 하는 핵심 문제는 단순한 상대 검색이 아니다. 사용자가 다음과 같은 확신을 얻도록 만드는 것이 핵심이다.

> 이 사람이라면 나도 부담 없이 같이 칠 수 있겠다.

모든 기능과 UX 결정은 초보자가 느끼는 다음 세 가지 불안을 줄이는 데 기여해야 한다.

- 내 실력이 부족해 상대방에게 민폐가 될 수 있다는 불안
- 처음 보는 사람에게 매칭을 신청하는 것에 대한 부담
- 코트를 어디서 어떻게 예약해야 하는지 모르는 데서 오는 부담

## 3. 주요 사용자

주요 사용자는 다음 특성을 가진 20~30대 테니스 입문자다.

- 구력 약 0~3년
- 레슨 또는 기초 랠리 경험이 있음
- 게임 경험이 적거나 없음
- 고정적으로 함께 칠 사람이 부족함
- 동호회 가입이나 기존 커뮤니티 참여가 부담스러움
- NTRP 등 전문적인 실력 등급에 익숙하지 않음
- 코트 검색 및 예약 경험이 적음

초보자가 이해하기 어려운 용어를 설명 없이 사용하지 않는다.

## 4. MVP 목표

MVP의 한 줄 정의는 다음과 같다.

> 나와 비슷한 수준의 테니스 초보자를 찾아 부담 없이 테니스 약속을 잡을 수 있는 서비스

MVP에서 우선 검증할 가설은 다음과 같다.

> 초보자가 상대방의 수준을 쉽게 이해할 수 있다면 실제로 처음 보는 사람에게 매칭을 신청하는가?

기능 수를 늘리는 것보다 아래 핵심 흐름을 완결하는 것을 우선한다.

1. 회원가입
2. 테니스 프로필 온보딩
3. 추천 매칭 탐색
4. 매칭 상세 확인
5. 같이 치기 신청
6. 모집자의 신청 확인 및 수락
7. 매칭 성사
8. 매칭 완료

Core MVP에서는 모집자가 외부에서 이미 예약한 코트 정보를 등록하거나, 코트가 아직 정해지지 않은 상태로 일정·활동 지역·모집 정보를 먼저 공개할 수 있어야 한다. `COURT_TBD` 매칭은 코트와 비용이 미정임을 명확히 표시하고, 수락된 참가자와 오픈채팅에서 조율한다. 제휴 코트 운영자의 시간대 등록, 예약 승인, 앱 결제와 정산은 후속 단계로 분리한다.

## 5. 제품 원칙

### 5.1 초보자의 언어를 사용한다

- NTRP나 숫자 레벨을 핵심 표현으로 사용하지 않는다.
- 실력은 구력만으로 판단하지 않는다.
- 랠리 경험, 게임 경험, 원하는 플레이를 함께 보여준다.
- `Level 2`보다 `랠리 성장중`처럼 현재 플레이 상태를 설명한다.

### 5.2 심리적 부담을 낮춘다

- 사용자가 매칭에 신청해도 되는지 쉽게 판단할 수 있어야 한다.
- 추천 결과에는 가능한 한 잘 맞는 이유를 설명한다.
- `초보자 환영`과 같이 신청 가능성을 명확하게 알려주는 정보를 우선한다.
- 불필요하게 긴 자기소개나 자유 입력을 요구하지 않는다.

### 5.3 입력보다 선택을 우선한다

- 온보딩과 매칭 등록에서는 선택형 입력을 우선한다.
- 꼭 필요한 경우에만 짧은 자유 입력을 사용한다.
- 온보딩은 약 30~45초 안에 끝낼 수 있는 범위를 유지한다.

### 5.4 실력과 매칭 목적을 분리한다

- 지속적인 사용자 상태인 `Player Skill`과 그날 원하는 활동인 `Match Purpose`를 별도 데이터로 관리한다.
- 사용자의 실력 프로필이 특정 매칭 목적에 의해 덮어써지지 않도록 한다.

### 5.5 설명 가능한 추천을 제공한다

- MVP 추천은 AI가 아닌 단순한 규칙 기반으로 구현한다.
- 내부 점수는 사용자에게 숫자로 노출하지 않는다.
- 사용자에게는 `잘 맞을 것 같아요`와 추천 이유를 자연어로 보여준다.

### 5.6 코트 확보 경로와 책임을 명확히 한다

- 외부에서 직접 예약한 코트와 Tennis Mate 제휴 코트를 구분하여 관리한다.
- 직접 예약 코트에는 `모집자가 코트를 예약했어요`라고 표시한다.
- 코트 미정 매칭에는 `코트와 비용을 함께 정해요`라고 표시하고, 예약 또는 비용 확정처럼 보이게 하지 않는다.
- 제휴 예약이 확인된 코트에만 `Tennis Mate에서 예약이 확인된 코트예요`라고 표시한다.
- 직접 예약 코트를 서비스가 검증하거나 보증한 것처럼 표현하지 않는다.
- Core MVP에서 직접 예약 코트의 참가비 정산은 서비스 밖에서 진행하며, 앱 결제 완료로 오해할 수 있는 UI를 제공하지 않는다.

### 5.7 코트 예약 승인과 참가 승인을 분리한다

- 코트 운영자는 코트 시간대의 예약 요청을 승인하거나 거절한다.
- 매칭 모집자는 함께 칠 참가자의 신청을 수락하거나 거절한다.
- 코트 예약이 확정되었다는 이유로 매칭 참가 신청을 자동 수락하지 않는다.
- 코트 시간대, 예약, 결제와 정산 상태를 서로 다른 개념으로 모델링한다.
- 동일한 코트 시간대가 중복 예약되지 않도록 서버에서 동시성을 제어한다.

### 5.8 Core MVP 확정 운영 정책을 따른다

- 첫 인증 공급자는 카카오 한 종류를 사용한다. 인증 라이브러리와 세부 설정은 프로젝트 초기화 시 공식 호환성을 확인한다.
- 소셜 표시명을 닉네임 기본값으로 제안하고 최초 진입에서 사용자가 확인하거나 수정하게 한다.
- 모집자는 매칭별 카카오 오픈채팅 링크를 등록하며 모집자와 `ACCEPTED` 신청자에게만 공개한다.
- 비로그인 사용자는 매칭 목록과 상세를 조회할 수 없다.
- 예상 1인 비용은 모집자를 포함한 예상 총 참여 인원으로 나누고 1원 단위 올림하며 사용자가 수정할 수 없다.
- 한 명 이상 수락된 매칭은 모집자가 조기 마감할 수 있다. 정원 충족·조기 마감·시작 시각 도달로 Match가 `CLOSED`가 되면 남은 대기 신청은 `CANCELLED`로 전환한다.
- 일정 종료 후 모집자가 완료를 확인하여 `COMPLETED`로 전환한다.
- 수락 후 참가 취소는 Core MVP 화면과 API에서 제공하지 않고 비공개 테스트에서는 운영 문의로 처리한다.
- 플레이 상태 자동 분류 규칙이 확정되기 전에는 `skillLabel`을 생성하거나 화면에 표시하지 않는다.
- 매칭 생성에는 `clientRequestId`를 사용하여 네트워크 재시도와 중복 클릭에 의한 중복 생성을 방지한다.
- 신고와 차단은 비공개 MVP에서 운영 문의로 처리하고 공개 출시 전에 최소 기능을 다시 검토한다.

## 6. MVP 범위 제한

다음 기능은 별도의 요구사항 변경이 있기 전까지 MVP에 추가하지 않는다.

- 실시간 채팅
- AI 영상 실력 분석
- 영상 업로드
- 복잡한 NTRP 자동 계산
- 클럽 및 동호회 기능
- 대회 및 랭킹
- 커뮤니티 게시판
- 복잡한 AI 추천 알고리즘
- 코트 운영자 입점 및 권한 인증
- 제휴 코트 예약 승인, 앱 결제, 환불 및 운영자 정산
- 참가자별 코트 비용 분할 결제

Core MVP에는 모집자가 외부에서 예약한 코트의 이름·위치·이용 일시·비용과 모집 정보를 등록하는 기능, 또는 코트와 비용을 미정으로 두고 일정·활동 지역·모집 정보를 등록하는 기능을 포함한다. 위에서 제외한 코트 운영자 및 결제 기능은 `Court Partner Pilot` 이후 단계로 취급한다.

`docs/03-1-court-partner-screen-spec.md`는 후속 단계의 화면과 데이터 확장 방향을 정의하는 설계 문서다. 별도의 구현 요청이 없으면 해당 문서의 기능을 Core MVP 범위로 간주하지 않는다.

새로운 기능을 제안하거나 구현하기 전에 반드시 다음을 설명한다.

1. 해결하려는 사용자 문제
2. 현재 MVP 목표에 필요한 이유
3. 기존 범위와 일정에 미치는 영향
4. 더 단순한 대안의 존재 여부

승인되지 않은 기능을 임의로 구현하지 않는다.

## 7. 문서 우선순위

구현 전에 현재 작업과 관련된 문서를 확인한다. 제품 설계 단계에서는 `docs/` 문서를 먼저 작성하고, `README.md`는 프로젝트 초기화와 기술 구성이 확정된 뒤 실행 가능한 내용으로 작성한다.

- 제품 배경과 핵심 가치: `docs/01-product-overview.md`
- MVP 요구사항과 정책: `docs/02-prd.md`
- 화면 및 사용자 흐름: `docs/03-screen-spec.md`
- 코트 운영자 및 제휴 코트 예약 화면: `docs/03-1-court-partner-screen-spec.md` (후속 단계)
- 데이터 모델: `docs/04-erd.md`
- API 계약: `docs/05-api-spec.md`
- 구현 순서와 완료 기준: `docs/06-development-plan.md`
- 프로젝트 소개 및 실행 방법: `README.md` (프로젝트 초기화 후 작성)

문서 간 내용이 충돌하면 임의로 선택하지 않는다. 충돌 위치, 가능한 선택지, 사용자 경험과 구현 범위에 미치는 영향을 먼저 보고한다.

요구사항을 변경해야 할 때는 코드만 수정하지 않고 관련 문서도 함께 갱신한다.

## 8. 기술 방향

M1에서 확정한 기본 기술 조합은 다음과 같다.

- Node.js 22
- Next.js 16 App Router, React 19, TypeScript strict
- Tailwind CSS 4
- 로컬 PostgreSQL 17
- Prisma ORM 7과 `@prisma/adapter-pg`
- Vitest
- Vercel과 Neon은 비공개 MVP 배포 시 생성 검토

Core MVP의 첫 소셜 인증 공급자는 카카오로 확정한다. Auth.js v5는 M2에서 실제 카카오 연동과 함께 호환성을 검증한 뒤 설치한다.

운영 DB 코드는 표준 PostgreSQL 연결을 유지한다. Neon 전용 API에 결합하지 않으며, 운영 리소스와 유료 환경은 별도 승인 전 생성하지 않는다. 다음 행동은 사전 합의 없이 진행하지 않는다.

- 핵심 프레임워크 또는 데이터베이스 교체
- 새로운 프로덕션 의존성 추가
- 외부 유료 서비스 도입
- 인증 방식 변경
- 배포 구조 변경

개인 개발자가 빠르게 완성하고 수정할 수 있는 단순한 구조를 우선한다. 최신 기술이라는 이유만으로 복잡한 구조나 도구를 도입하지 않는다.

## 9. 구현 원칙

- 모바일 사용을 우선하여 화면을 설계한다.
- 기존 코드와 사용자의 변경사항을 보존한다.
- 현재 요구사항에 필요하지 않은 추상화는 만들지 않는다.
- 읽기 쉬운 이름과 작은 책임 단위를 사용한다.
- 동일한 비즈니스 규칙을 여러 위치에 중복 구현하지 않는다.
- UI 문구는 초보자가 이해하기 쉬운 표현을 사용한다.
- 로딩, 빈 화면, 오류, 권한 없음 상태를 정상 흐름과 함께 설계한다.
- 사용자 입력은 클라이언트와 서버에서 모두 검증한다.
- 날짜, 시간, 지역, 모집 상태처럼 매칭 결과에 영향을 주는 값은 명시적으로 모델링한다.
- 코트 정보에는 예약 출처를 명시적으로 모델링하고, 출처별 책임과 표시 문구를 분리한다.
- 직접 예약 코트와 제휴 코트에 동일한 검증 배지를 사용하지 않는다.
- 제휴 코트 기능에서는 코트 운영자의 예약 승인과 모집자의 참가 승인을 별도 권한과 상태로 처리한다.
- 예약 상태와 결제 상태를 하나의 열거형이나 화면 상태로 합치지 않는다.
- 개인정보와 인증 정보는 로그, 코드, 저장소에 노출하지 않는다.
- 비밀값은 환경 변수로 관리하고 예제 파일에는 실제 값을 넣지 않는다.

## 10. 작업 방식

### 구현 전

1. 관련 문서와 기존 코드를 확인한다.
2. 요청 범위와 완료 조건을 짧게 정리한다.
3. 미확정 정책이 구현 결과를 크게 바꾸는 경우 먼저 질문한다.
4. 작은 작업은 바로 구현하고, 여러 영역에 걸친 변경은 계획을 먼저 제시한다.

### 구현 중

1. 한 번에 하나의 검토 가능한 개발 단위로 작업한다.
2. 요청 범위를 벗어난 리팩터링을 섞지 않는다.
3. 데이터 모델, API, 화면의 상태 정의가 서로 일치하도록 유지한다.
4. 중요한 판단이나 가정은 관련 문서에 기록한다.

### 구현 후

1. 변경된 동작을 직접 검증한다.
2. 관련 테스트, 타입 검사, 린트 및 빌드를 실행한다.
3. 변경 파일, 구현 결과, 검증 결과, 남은 위험을 보고한다.
4. 검증을 실행하지 못했거나 실패한 항목은 숨기지 않고 이유를 설명한다.

## 11. 테스트 및 검증 기준

현재 실제 `package.json`의 기본 검증 명령은 다음과 같다.

- 린트: `npm run lint`
- TypeScript 타입 검사: `npm run typecheck`
- 단위 테스트: `npm run test`
- 프로덕션 빌드: `npm run build`
- 전체 검증: `npm run check`
- Prisma 스키마 검증: `npm run db:validate`
- Prisma Client 생성: `npm run db:generate`

DB 연결과 마이그레이션 검증 전에는 `docker compose up -d`로 로컬 PostgreSQL을 실행하고 `.env`를 설정한다.

최소 검증 항목은 다음과 같다.

- 린트
- TypeScript 타입 검사
- 단위 또는 통합 테스트
- 프로덕션 빌드
- 변경한 핵심 사용자 흐름의 수동 확인

핵심 흐름을 변경할 때는 정상 케이스뿐 아니라 다음 상태도 확인한다.

- 입력값 누락 또는 형식 오류
- 로그인하지 않은 사용자
- 권한이 없는 사용자
- 이미 마감되거나 취소된 매칭
- 시작 시간이 지나 성사되지 않고 종료된 매칭
- 중복 신청
- 빈 목록
- 네트워크 또는 서버 오류

## 12. 완료 정의

작업은 다음 조건을 모두 만족해야 완료된 것으로 간주한다.

- 요청한 사용자 행동이 처음부터 끝까지 동작한다.
- 관련 제품 문서와 구현이 일치한다.
- 모바일 기준으로 핵심 UI를 사용할 수 있다.
- 필요한 로딩, 빈 화면, 오류 상태가 처리된다.
- 권한 및 입력 검증이 서버에서도 적용된다.
- 관련 검증 명령이 통과한다.
- 범위를 벗어난 기능이나 의존성이 추가되지 않았다.
- 변경 내용과 미해결 항목이 명확하게 보고되었다.

## 13. 미확정 정책 처리

다음 항목은 상세 설계 또는 PRD에서 확정되기 전까지 임의로 결정하지 않는다.

- 성별 및 연령 정보의 수집 여부
- 후기 제공 여부
- 노쇼 정책
- 외부 예약 코트의 취소 및 정보 오류 처리 정책
- 제휴 코트의 예약 승인, 결제 제한 시간, 환불 및 정산 정책
- 제휴 코트 시간대의 임시 점유, 승인 제한 시간과 중복 요청 정책
- 공개 후 매칭 일정과 코트 정보 변경 정책
- 수락 후 참가 취소를 정식 기능으로 제공할 때의 상태, 사유와 이력 정책
- 플레이 상태 자동 분류 규칙
- 공개 출시 전 신고·차단·노쇼 정책

미확정 정책 때문에 구현이 막히면 다음 형식으로 선택지를 제시한다.

1. 권장안과 그 이유
2. 대안
3. 사용자 경험 차이
4. 개발 범위 및 데이터 모델에 미치는 영향

## 14. 지침 유지 관리

반복되는 오류, 잘못된 가정 또는 새로운 공통 규칙이 확인되면 해당 작업에서 끝내지 말고 이 파일의 갱신을 제안한다.

제품 상세 내용이 길어질 경우 이 파일에 모두 추가하지 않는다. `AGENTS.md`는 짧고 실행 가능한 공통 규칙을 유지하고, 상세 정책은 가장 가까운 `docs/` 문서에 기록한다.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
